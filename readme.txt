-- LIST OF DEPENDENCIES NECESSARY --
numpy
pandas
matplotlib
all existing in "dependencies.txt"

to install them all at once, open terminal and run:
pip install -r requirements.txt

to check installed packages run:
pip list